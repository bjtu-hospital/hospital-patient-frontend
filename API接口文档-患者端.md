# 患者端API接口文档


## 📌 接口规范

### 基础信息
- **Base URL**: `http://localhost:8000` (开发环境)
- **请求头**: `Content-Type: application/json`
- **认证方式**: `Authorization: Bearer {token}`

### 统一响应格式
```json
{
  "code": 0,           // 0表示成功，其他表示错误
  "message": "操作成功",
  "data": {}           // 具体数据
}
```

### 错误码说明
- `0` - 成功
- `400` - 参数错误
- `401` - 未登录或token过期
- `403` - 无权限
- `500` - 服务器错误

---

## 1️⃣ 认证模块

### 1.1 用户注册
**接口**: `POST /auth/patient/register`  
**说明**: 新用户注册  
**是否需要登录**: 否

**请求参数**:
```json
{
  "studentId": "23301087",      // 学号，8位数字
  "realName": "张三",            // 真实姓名
  "phone": "13800138000",       // 手机号
  "idCard": "110101199001011234", // 身份证号
  "password": "123456"           // 密码，至少6位
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "注册成功",
  "data": {
    "userId": "user_001"
  }
}
```

---

### 1.2 用户登录
**接口**: `POST /auth/patient/login`  
**说明**: 用户登录  
**是否需要登录**: 否

**请求参数**:
```json
{
  "identifier": "23301087",  // 学号或手机号
  "password": "123456"       // 密码
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "userInfo": {
      "id": "user_001",
      "studentId": "23301087",
      "realName": "张三",
      "phone": "13800138000",
      "avatar": "https://...",
      "verified": true  // 是否已认证
    }
  }
}
```

---

### 1.3 学号认证
**接口**: `POST /auth/patient/verify`  
**说明**: 验证学号和姓名是否匹配（对接学校系统）  
**是否需要登录**: 是

**请求参数**:
```json
{
  "studentId": "23301087",
  "realName": "张三"
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "认证成功",
  "data": {
    "verified": true,
    "info": {
      "department": "计算机学院",
      "grade": "2023级"
    }
  }
}
```

---

### 1.4 退出登录
**接口**: `POST /auth/logout`  
**说明**: 退出登录  
**是否需要登录**: 是

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "退出成功"
}
```

---

## 2️⃣ 用户信息模块

### 2.1 获取个人信息
**接口**: `GET /patient/profile`  
**说明**: 获取当前登录用户的个人信息  
**是否需要登录**: 是

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": "user_001",
    "studentId": "23301087",
    "realName": "张三",
    "phone": "13800138000",
    "idCard": "110101199001011234",
    "avatar": "https://...",
    "verified": true,
    "createdAt": "2024-10-01 10:00:00"
  }
}
```

---

### 2.2 更新个人信息
**接口**: `PUT /patient/profile`  
**说明**: 更新个人信息  
**是否需要登录**: 是

**请求参数**:
```json
{
  "phone": "13900139000",  // 可选
  "avatar": "https://..."  // 可选
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "更新成功"
}
```

---

## 3️⃣ 就诊人管理模块

### 3.1 获取就诊人列表
**接口**: `GET /patient/patients`  
**说明**: 获取当前用户的所有就诊人  
**是否需要登录**: 是

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": "patient_001",
      "name": "张三",
      "idCard": "110101199001011234",
      "phone": "13800138000",
      "relation": "本人",  // 本人/父母/子女/配偶/其他
      "isDefault": true,   // 是否默认就诊人
      "createdAt": "2024-10-01 10:00:00"
    }
  ]
}
```

---

### 3.2 添加就诊人
**接口**: `POST /patient/patients`  
**说明**: 添加新的就诊人  
**是否需要登录**: 是

**请求参数**:
```json
{
  "name": "李四",
  "idCard": "110101199501011234",
  "phone": "13900139000",
  "relation": "子女"
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "添加成功",
  "data": {
    "id": "patient_002"
  }
}
```

---

### 3.3 更新就诊人
**接口**: `PUT /patient/patients/{patientId}`  
**说明**: 更新就诊人信息  
**是否需要登录**: 是

**请求参数**:
```json
{
  "phone": "13900139001"
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "更新成功"
}
```

---

### 3.4 删除就诊人
**接口**: `DELETE /patient/patients/{patientId}`  
**说明**: 删除就诊人  
**是否需要登录**: 是

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "删除成功"
}
```

---

## 4️⃣ 预约挂号模块

### 4.1 获取医院列表
**接口**: `GET /patient/hospitals`  
**说明**: 获取可预约的医院列表  
**是否需要登录**: 否

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": "hospital_001",
      "name": "北京交通大学校医院（本部）",
      "address": "北京市海淀区上园村3号",
      "level": "三级",
      "type": "综合医院",
      "phone": "010-12345678",
      "openTime": "08:00-18:00",
      "image": "https://...",
      "departmentCount": 15,
      "doctorCount": 48
    }
  ]
}
```

---

### 4.2 获取科室列表
**接口**: `GET /patient/hospitals/{hospitalId}/departments`  
**说明**: 获取指定医院的科室列表  
**是否需要登录**: 否

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": "dept_001",
      "name": "内科",
      "description": "擅长治疗...",
      "doctorCount": 8,
      "availableSlots": 20
    }
  ]
}
```

---

### 4.3 获取医生排班
**接口**: `GET /patient/schedules`  
**说明**: 获取医生的排班信息  
**是否需要登录**: 否

**请求参数**:
```
hospitalId: hospital_001  // 医院ID
departmentId: dept_001    // 科室ID
date: 2024-11-05          // 日期，可选，默认今天起7天内
```

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": "schedule_001",
      "doctor": {
        "id": "doctor_001",
        "name": "张医生",
        "title": "主任医师",
        "avatar": "https://...",
        "specialty": "擅长心血管疾病...",
        "department": "内科"
      },
      "date": "2024-11-05",
      "period": "上午",  // 上午/下午/晚上
      "timeSlots": [
        {
          "id": "slot_001",
          "startTime": "08:00",
          "endTime": "08:30",
          "available": true,
          "price": 50,
          "type": "普通号"  // 普通号/专家号
        }
      ]
    }
  ]
}
```

---

### 4.4 创建预约
**接口**: `POST /patient/appointments`  
**说明**: 创建新的预约  
**是否需要登录**: 是

**请求参数**:
```json
{
  "scheduleId": "schedule_001",
  "slotId": "slot_001",
  "patientId": "patient_001",
  "symptoms": "头疼发烧"  // 可选，症状描述
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "预约成功",
  "data": {
    "id": "appointment_001",
    "orderNo": "20241105001",
    "queueNumber": "15",  // 排队号
    "needPay": true,
    "payAmount": 50
  }
}
```

---

### 4.5 获取我的预约
**接口**: `GET /patient/appointments`  
**说明**: 获取我的预约记录  
**是否需要登录**: 是

**请求参数**:
```
status: pending    // 可选，pending/completed/cancelled
page: 1
pageSize: 10
```

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 20,
    "list": [
      {
        "id": "appointment_001",
        "orderNo": "20241105001",
        "hospitalName": "北京交通大学校医院（本部）",
        "departmentName": "内科",
        "doctorName": "张医生",
        "doctorTitle": "主任医师",
        "appointmentDate": "2024-11-05",
        "appointmentTime": "上午 08:00-08:30",
        "patientName": "张三",
        "queueNumber": "15",
        "price": 50,
        "status": "pending",  // pending待就诊/completed已完成/cancelled已取消
        "canCancel": true,
        "canReschedule": true,
        "createdAt": "2024-11-01 10:00:00"
      }
    ]
  }
}
```

---

### 4.6 取消预约
**接口**: `PUT /patient/appointments/{appointmentId}/cancel`  
**说明**: 取消预约  
**是否需要登录**: 是

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "取消成功"
}
```

---

### 4.7 改约
**接口**: `PUT /patient/appointments/{appointmentId}/reschedule`  
**说明**: 改约到其他时间  
**是否需要登录**: 是

**请求参数**:
```json
{
  "newScheduleId": "schedule_002",
  "newSlotId": "slot_005"
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "改约成功"
}
```

---

### 4.8 候补预约
**接口**: `POST /patient/waitlist`  
**说明**: 号源已满时，加入候补列表  
**是否需要登录**: 是

**请求参数**:
```json
{
  "scheduleId": "schedule_001",
  "patientId": "patient_001"
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "已加入候补队列",
  "data": {
    "waitlistId": "waitlist_001",
    "position": 5  // 候补位置
  }
}
```

---

## 5️⃣ 健康档案模块

### 5.1 获取健康档案
**接口**: `GET /patient/health-records`  
**说明**: 获取健康档案信息  
**是否需要登录**: 是

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "basicInfo": {
      "height": 175,
      "weight": 70,
      "bloodType": "A型",
      "allergies": ["青霉素"]
    },
    "medicalHistory": [
      {
        "id": "record_001",
        "date": "2024-10-01",
        "hospitalName": "北交大校医院",
        "departmentName": "内科",
        "doctorName": "张医生",
        "diagnosis": "感冒",
        "treatment": "开了药"
      }
    ]
  }
}
```

---

### 5.2 获取检验报告
**接口**: `GET /patient/reports`  
**说明**: 获取检验报告列表  
**是否需要登录**: 是

**请求参数**:
```
page: 1
pageSize: 10
```

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 10,
    "list": [
      {
        "id": "report_001",
        "type": "血常规",
        "date": "2024-10-01",
        "hospitalName": "北交大校医院",
        "status": "已出结果",
        "pdfUrl": "https://..."
      }
    ]
  }
}
```

---

## 6️⃣ 消息模块

### 6.1 获取消息列表
**接口**: `GET /patient/messages`  
**说明**: 获取消息列表  
**是否需要登录**: 是

**请求参数**:
```
page: 1
pageSize: 10
```

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 20,
    "unreadCount": 5,
    "list": [
      {
        "id": "msg_001",
        "type": "appointment",  // appointment预约/system系统
        "title": "预约提醒",
        "content": "您预约的张医生门诊将在明天上午8点开始",
        "isRead": false,
        "createdAt": "2024-11-01 10:00:00"
      }
    ]
  }
}
```

---

### 6.2 标记已读
**接口**: `PUT /patient/messages/{messageId}/read`  
**说明**: 标记消息为已读  
**是否需要登录**: 是

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "success"
}
```

---

### 6.3 删除消息
**接口**: `DELETE /patient/messages/{messageId}`  
**说明**: 删除消息  
**是否需要登录**: 是

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "删除成功"
}
```

---

## 7️⃣ 科室专家模块

### 7.1 获取科室专家列表
**接口**: `GET /patient/doctors`  
**说明**: 获取科室专家列表  
**是否需要登录**: 否

**请求参数**:
```
departmentId: dept_001  // 可选
keyword: 张              // 可选，搜索关键词
page: 1
pageSize: 10
```

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 30,
    "list": [
      {
        "id": "doctor_001",
        "name": "张医生",
        "title": "主任医师",
        "avatar": "https://...",
        "departmentName": "内科",
        "specialty": "擅长心血管疾病治疗...",
        "experience": "从医20年",
        "rating": 4.8,
        "patientCount": 1234
      }
    ]
  }
}
```

---

### 7.2 获取医生详情
**接口**: `GET /patient/doctors/{doctorId}`  
**说明**: 获取医生详细信息  
**是否需要登录**: 否

**请求参数**: 无

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": "doctor_001",
    "name": "张医生",
    "title": "主任医师",
    "avatar": "https://...",
    "departmentName": "内科",
    "specialty": "擅长心血管疾病治疗...",
    "education": "北京医科大学",
    "experience": "从医20年",
    "achievements": ["获得XX奖项"],
    "rating": 4.8,
    "patientCount": 1234,
    "schedules": [...]  // 可预约的排班
  }
}
```

---

## 8️⃣ 意见反馈模块

### 8.1 提交意见反馈
**接口**: `POST /patient/feedback`  
**说明**: 提交意见反馈  
**是否需要登录**: 是

**请求参数**:
```json
{
  "type": "suggestion",  // suggestion建议/complaint投诉/other其他
  "content": "希望增加...",
  "contact": "13800138000",  // 可选
  "images": ["https://..."]  // 可选，图片URLs
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "提交成功",
  "data": {
    "id": "feedback_001"
  }
}
```

---

### 8.2 获取反馈列表
**接口**: `GET /patient/feedback`  
**说明**: 获取我的反馈记录  
**是否需要登录**: 是

**请求参数**:
```
page: 1
pageSize: 10
```

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 5,
    "list": [
      {
        "id": "feedback_001",
        "type": "suggestion",
        "content": "希望增加...",
        "status": "pending",  // pending处理中/replied已回复
        "reply": null,
        "createdAt": "2024-11-01 10:00:00"
      }
    ]
  }
}
```

---

## 9️⃣ AI助手模块

### 9.1 智能推荐医生
**接口**: `POST /patient/ai/recommend-doctor`  
**说明**: 根据症状推荐医生  
**是否需要登录**: 是

**请求参数**:
```json
{
  "symptoms": "头疼、发烧、咳嗽",
  "duration": "3天"  // 可选
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "suggestedDepartment": "呼吸内科",
    "reason": "根据您的症状，建议挂呼吸内科",
    "doctors": [
      {
        "id": "doctor_001",
        "name": "张医生",
        "title": "主任医师",
        "matchScore": 95
      }
    ]
  }
}
```

---

### 9.2 智能问答
**接口**: `POST /patient/ai/chat`  
**说明**: AI健康咨询  
**是否需要登录**: 是

**请求参数**:
```json
{
  "question": "感冒了应该注意什么？"
}
```

**响应数据**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "answer": "感冒期间应该注意..."
  }
}
```

---

## 📋 接口清单汇总

### 认证模块（4个）
- ✅ POST /auth/patient/register - 注册
- ✅ POST /auth/patient/login - 登录
- ✅ POST /auth/patient/verify - 学号认证
- ✅ POST /auth/logout - 退出登录

### 用户信息（2个）
- ✅ GET /patient/profile - 获取个人信息
- ✅ PUT /patient/profile - 更新个人信息

### 就诊人管理（4个）
- ✅ GET /patient/patients - 获取就诊人列表
- ✅ POST /patient/patients - 添加就诊人
- ✅ PUT /patient/patients/{id} - 更新就诊人
- ✅ DELETE /patient/patients/{id} - 删除就诊人

### 预约挂号（8个）
- ✅ GET /patient/hospitals - 获取医院列表
- ✅ GET /patient/hospitals/{id}/departments - 获取科室列表
- ✅ GET /patient/schedules - 获取医生排班
- ✅ POST /patient/appointments - 创建预约
- ✅ GET /patient/appointments - 获取我的预约
- ✅ PUT /patient/appointments/{id}/cancel - 取消预约
- ✅ PUT /patient/appointments/{id}/reschedule - 改约
- ✅ POST /patient/waitlist - 候补预约

### 健康档案（2个）
- ✅ GET /patient/health-records - 获取健康档案
- ✅ GET /patient/reports - 获取检验报告

### 消息管理（3个）
- ✅ GET /patient/messages - 获取消息列表
- ✅ PUT /patient/messages/{id}/read - 标记已读
- ✅ DELETE /patient/messages/{id} - 删除消息

### 科室专家（2个）
- ✅ GET /patient/doctors - 获取医生列表
- ✅ GET /patient/doctors/{id} - 获取医生详情

### 意见反馈（2个）
- ✅ POST /patient/feedback - 提交反馈
- ✅ GET /patient/feedback - 获取反馈记录

### AI助手（2个）
- ✅ POST /patient/ai/recommend-doctor - 智能推荐医生
- ✅ POST /patient/ai/chat - AI问答

---

## 📝 注意事项

### 给后端开发的说明
1. **所有需要登录的接口**，前端会在请求头带上 `Authorization: Bearer {token}`
2. **统一返回格式**，便于前端统一处理
3. **分页参数**统一为 `page` 和 `pageSize`
4. **日期格式**统一为 `YYYY-MM-DD` 或 `YYYY-MM-DD HH:mm:ss`
5. **ID字段**统一使用字符串类型

### 给前端开发的说明
1. 开发阶段使用**模拟数据**
2. 按照接口文档定义的格式编写代码
3. 后端接口ready后，只需修改 `src/api/*.js` 中的调用即可
4. Token在登录成功后保存到本地，每次请求自动带上

---

## 🔄 后续对接流程

1. **后端开发完一个接口** → 通知前端
2. **前端修改对应的API调用** → 去掉模拟数据，调用真实接口
3. **联调测试** → 发现问题及时沟通
4. **完成对接** → 进入下一个接口

---

更新记录：
- 2024-11-02：初始版本，定义全部29个接口

