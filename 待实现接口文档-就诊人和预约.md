# 患者端待实现接口文档 - 就诊人管理和预约功能

## 📋 接口实现状态总览（基于 Swagger 文档）

### ✅ 已实现的接口（9个）

**预约管理（3个）：**
- `POST /patient/appointments` - 创建预约
- `GET /patient/appointments` - 获取预约列表
- `PUT /patient/appointments/{appointmentId}/cancel` - 取消预约

**就诊人管理（6个）：**
- `GET /patient/patients` - 获取就诊人列表 ✅
- `POST /patient/patients` - 添加就诊人 ✅
- `PUT /patient/patients/{patient_id}` - 更新就诊人 ✅
- `DELETE /patient/patients/{patient_id}` - 删除就诊人 ✅
- `PUT /patient/patients/{patient_id}/set-default` - 设置默认就诊人 ✅
- `GET /patient/patients/default` - 获取默认就诊人 ✅

### ❌ 待实现的接口（1个）

**预约管理（1个）：**
- `GET /patient/appointments/{appointmentId}` - 预约详情（可选，列表接口已返回完整数据）



## 一、就诊人管理接口（✅ 已完成集成）

> **集成状态：** 所有就诊人管理接口已在 `src/api/user.js` 中完成集成
> **字段映射：** 后端 snake_case ↔ 前端 camelCase 自动转换
> **新增字段：** identifier（学号/工号，明文）、idCard（身份证，脱敏）

### 1.1 获取就诊人列表 ✅

**接口路径：** `GET /patient/patients`

**前端函数：** `getPatients()` (src/api/user.js)

**请求头：**
```
Authorization: Bearer {token}
```

**请求参数：** 无

**后端返回格式：**
```json
{
  "code": 0,
  "message": [
    {
      "relation_id": "rel_001",
      "patient": {
        "patient_id": "p_123",
        "identifier": "1120203101",
        "real_name": "张三",
        "id_card": "110101********1234",
        "phone_number": "138****8000"
      },
      "relation_type": "本人",
      "is_default": true,
      "remark": "",
      "created_at": "2024-01-01T10:00:00Z",
      "updated_at": "2024-01-01T10:00:00Z"
    }
  ],
  "success": true
}
```

**前端转换后格式：**
```javascript
{
  total: 1,
  patients: [
    {
      relationId: "rel_001",
      patientId: "p_123",
      identifier: "1120203101",
      name: "张三",
      idCard: "110101********1234",
      phone: "138****8000",
      relationType: "本人",
      isDefault: true,
      remark: "",
      createdAt: "2024-01-01T10:00:00Z",
      updatedAt: "2024-01-01T10:00:00Z"
    }
  ]
}
```

**字段说明：**
- `relationId`: 关系记录ID（前端业务主键）
- `patientId`: 患者ID（后端患者表主键）
- `identifier`: 学号/工号（明文，无脱敏）
- `name`: 真实姓名
- `idCard`: 身份证号（脱敏：前6位+8个*+后4位）
- `phone`: 手机号（脱敏：前3位+4个*+后4位）
- `relationType`: 与当前用户关系（本人/父母/配偶/子女/其他）
- `isDefault`: 是否为默认就诊人
- `remark`: 备注信息

---

### 1.2 添加就诊人 ✅

**接口路径：** `POST /patient/patients`

**前端函数：** `addPatient(data)` (src/api/user.js)

**请求头：**
```
Authorization: Bearer {token}
Content-Type: application/json
```

**前端请求数据：**
```javascript
{
  identifier: "1120203101",
  name: "张三",
  idCard: "110101199001011234",
  phone: "13800138000",
  relationType: "本人",
  isDefault: false,
  remark: ""
}
```

**转换后的后端请求体：**
```json
{
  "identifier": "1120203101",
  "real_name": "张三",
  "id_card": "110101199001011234",
  "phone_number": "13800138000",
  "relation_type": "本人",
  "is_default": false,
  "remark": ""
}
```

**后端返回格式：**
```json
{
  "code": 0,
  "message": {
    "relation_id": "rel_003",
    "patient_id": "p_789"
  },
  "success": true
}
```

**前端转换后：**
```javascript
{
  relationId: "rel_003",
  patientId: "p_789"
}
```

**验证规则（前端）：**
- `identifier`: 必需，8-12位数字（学号/工号格式）
- `name`: 必需，2-20个汉字
- `idCard`: 必需，18位身份证号
- `phone`: 必需，11位手机号
- `relationType`: 必需，枚举值（本人/父母/配偶/子女/其他）

---

### 1.3 更新就诊人 ✅

**接口路径：** `PUT /patient/patients/{patient_id}`

**前端函数：** `updatePatient(patientId, data)` (src/api/user.js)

**URL参数：**
- `patient_id`: 关系记录ID（relationId）

**请求头：**
```
Authorization: Bearer {token}
Content-Type: application/json
```

**前端请求数据（仅支持这两个字段）：**
```javascript
{
  relationType: "父母",
  remark: "备注信息"
}
```

**转换后的后端请求体：**
```json
{
  "relation_type": "父母",
  "remark": "备注信息"
}
```

**后端返回格式：**
```json
{
  "code": 0,
  "message": "更新成功",
  "success": true
}
```

**⚠️ 重要限制：**
- **仅允许更新 `relation_type` 和 `remark` 字段**
- 患者基本信息（identifier、name、idCard、phone）不可修改
- 如需修改基本信息，需删除后重新添加

---

### 1.4 删除就诊人 ✅

**接口路径：** `DELETE /patient/patients/{patient_id}`

**前端函数：** `deletePatient(patientId)` (src/api/user.js)

**URL参数：**
- `patient_id`: 关系记录ID（relationId）

**请求头：**
```
Authorization: Bearer {token}
```

**前端验证逻辑：**
```javascript
// 前端会先检查是否为默认就诊人
if (patient.isDefault) {
  throw new Error('无法删除默认就诊人，请先设置其他就诊人为默认')
}
```

**后端返回格式：**
```json
{
  "code": 0,
  "message": "删除成功",
  "success": true
}
```
```

**业务规则：**
- 默认就诊人（`isDefault: true`）不允许删除
- 前端会在调用前做校验
- 如需删除默认就诊人，需先将其他就诊人设为默认

---

### 1.5 设置默认就诊人 ✅

**接口路径：** `PUT /patient/patients/{patient_id}/set-default`

**前端函数：** `setDefaultPatient(patientId)` (src/api/user.js)

**URL参数：**
- `patient_id`: 关系记录ID（relationId）

**请求头：**
```
Authorization: Bearer {token}
```

**后端返回格式：**
```json
{
  "code": 0,
  "message": "设置成功",
  "success": true
}
```

**业务规则：**
- 设置新的默认就诊人时，后端自动取消其他就诊人的默认状态
- 同一用户同时只能有一个默认就诊人

---

### 1.6 获取默认就诊人 ✅

**接口路径：** `GET /patient/patients/default`

**前端函数：** `getDefaultPatient()` (src/api/user.js)

**请求头：**
```
Authorization: Bearer {token}
```

**后端返回格式（有默认就诊人）：**
```json
{
  "code": 0,
  "message": {
    "relation_id": "rel_001",
    "patient": {
      "patient_id": "p_123",
      "identifier": "1120203101",
      "real_name": "张三",
      "id_card": "110101********1234",
      "phone_number": "138****8000"
    },
    "relation_type": "本人",
    "is_default": true,
    "remark": "",
    "created_at": "2024-01-01T10:00:00Z",
    "updated_at": "2024-01-01T10:00:00Z"
  },
  "success": true
}
```

**后端返回格式（无默认就诊人）：**
```json
{
  "code": 0,
  "message": null,
  "success": true
}
```

**前端转换后格式：**
```javascript
// 有默认就诊人时
{
  relationId: "rel_001",
  patientId: "p_123",
  identifier: "1120203101",
  name: "张三",
  idCard: "110101********1234",
  phone: "138****8000",
  relationType: "本人",
  isDefault: true,
  remark: "",
  createdAt: "2024-01-01T10:00:00Z",
  updatedAt: "2024-01-01T10:00:00Z"
}

// 无默认就诊人时
null
```

---

## 二、预约详情接口（可选）

### 2.1 预约详情
```
Authorization: Bearer {token}
```

**返回格式：**
```json
{
  "code": 0,
  "message": "设置成功"
}
```
**接口路径：** `GET /patient/appointments/{appointmentId}`

**前端函数：** 暂未实现（列表接口 `getMyAppointments()` 已返回完整数据）

**URL参数：**
- `appointmentId`: 预约ID（String/Number类型）

**请求头：**
```
Authorization: Bearer {token}
```

**后端返回格式：**
```json
{
  "code": 0,
  "message": {
    "appointment_id": "appt_001",
    "order_no": "202512011234567890",
    "hospital_id": "hospital_001",
    "hospital_name": "北京交通大学校医院（本部）",
    "department_id": "dept_005",
    "department_name": "心内科",
    "doctor_id": "doctor_013",
    "doctor_name": "张医生",
    "doctor_title": "主治医师",
    "patient": {
      "relation_id": "rel_001",
      "patient_id": "p_123",
      "identifier": "1120203101",
      "real_name": "张三",
      "phone_number": "138****8000"
    },
    "schedule_id": "schedule_12345",
    "appointment_date": "2025-12-03",
    "time_slot": "08:00-08:30",
    "queue_number": 15,
    "fee": 50.0,
    "symptoms": "头痛发热",
    "status": "confirmed",
    "payment_status": "paid",
    "created_at": "2025-12-01T10:30:00Z",
    "updated_at": "2025-12-01T10:30:00Z"
  },
  "success": true
}
```

**说明：**
- 此接口为**可选实现**
- `GET /patient/appointments` 列表接口已返回完整预约信息
- 如需单独查看预约详情页，可实现此接口

---

## 三、字段映射说明

### 3.1 后端 → 前端字段映射

**就诊人相关：**
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| relation_id | relationId | 关系记录ID（业务主键） |
| patient.patient_id | patientId | 患者ID（患者表主键） |
| patient.identifier | identifier | 学号/工号（明文） |
| patient.real_name | name | 真实姓名 |
| patient.id_card | idCard | 身份证号（脱敏） |
| patient.phone_number | phone | 手机号（脱敏） |
| relation_type | relationType | 关系类型 |
| is_default | isDefault | 是否默认 |
| created_at | createdAt | 创建时间 |
| updated_at | updatedAt | 更新时间 |

**预约相关：**
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| appointment_id | id | 预约ID |
| order_no | orderNo | 订单号 |
| hospital_id | hospitalId | 医院ID |
| hospital_name | hospitalName | 医院名称 |
| department_id | departmentId | 科室ID |
| department_name | departmentName | 科室名称 |
| doctor_id | doctorId | 医生ID |
| doctor_name | doctorName | 医生姓名 |
| doctor_title | doctorTitle | 医生职称 |
| appointment_date | appointmentDate | 预约日期 |
| time_slot | appointmentTime | 时间段 |
| queue_number | queueNumber | 排队号 |
| payment_status | paymentStatus | 支付状态 |
| created_at | createdAt | 创建时间 |
| updated_at | updatedAt | 更新时间 |

### 3.2 前端 → 后端字段映射

**添加就诊人请求：**
| 前端字段 | 后端字段 | 必需 | 说明 |
|---------|---------|-----|------|
| identifier | identifier | 是 | 学号/工号 |
| name | real_name | 是 | 真实姓名 |
| idCard | id_card | 是 | 身份证号（18位，明文） |
| phone | phone_number | 是 | 手机号（11位，明文） |
| relationType | relation_type | 是 | 关系类型 |
| isDefault | is_default | 否 | 是否默认（默认false） |
| remark | remark | 否 | 备注信息 |

**更新就诊人请求：**
| 前端字段 | 后端字段 | 说明 |
|---------|---------|------|
| relationType | relation_type | 关系类型（可选） |
| remark | remark | 备注信息（可选） |

**注意：** 患者基本信息（identifier、name、idCard、phone）不可通过更新接口修改

---

## 四、错误码说明

| 错误码 | 说明 | 处理建议 |
|-------|------|---------|
| 0 | 成功 | - |
| 98 | HTTP异常（404等） | 检查接口路径是否正确 |
| 105 | Token无效或已失效 | 清除本地token，跳转登录页 |
| 400 | 参数错误 | 检查请求参数格式 |
| 403 | 无权访问 | 检查用户权限 |
| 404 | 资源不存在 | 检查ID是否正确 |
| 500 | 服务器错误 | 联系技术支持 |

---

## 五、注意事项

### 5.1 命名规范
- **后端返回：** 使用 snake_case（下划线命名）
- **前端使用：** 自动转换为 camelCase（驼峰命名）
- **前端请求：** 发送前自动转换为 snake_case

### 5.2 数据脱敏规则
- **identifier（学号/工号）：** 明文显示，无脱敏
- **idCard（身份证号）：** 前6位 + 8个* + 后4位（如：110101********1234）
- **phone（手机号）：** 前3位 + 4个* + 后4位（如：138****8000）

### 5.3 业务规则
- 默认就诊人不允许删除，需先设置其他就诊人为默认
- 同一用户同时只能有一个默认就诊人
- 设置新默认就诊人时，后端自动取消其他就诊人的默认状态
- 就诊人基本信息（identifier、name、idCard、phone）创建后不可修改
- 仅允许修改关系类型（relationType）和备注（remark）

### 5.4 前端实现文件
- **API层：** `src/api/user.js` - 包含所有就诊人管理函数
- **Mock数据：** `src/pages/profile/user-mock.js` - 开发测试用Mock数据
- **状态管理：** `src/stores/user.js` - 用户状态管理（如需）

### 5.5 集成状态
- ✅ 所有6个就诊人管理接口已在 `src/api/user.js` 中完成集成
- ✅ 字段映射逻辑已实现（snake_case ↔ camelCase）
- ✅ Mock数据已更新为最新结构
- ⏳ UI组件需更新以显示 identifier 字段
- ⏳ 需使用真实后端API进行测试验证
